{
  "name": "warehouse-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm install && npm run start"
  },
  "engines": {
    "node": ">=20"
  }
}
